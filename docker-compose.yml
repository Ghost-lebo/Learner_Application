services:
  backend:
    build: ./backend
    container_name: learner_backend
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    container_name: learner_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
